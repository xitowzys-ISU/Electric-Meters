from dataclasses import dataclass


@dataclass
class ProjectId:
    """
    Dataclass
    ----------
    The data class used to store the project id

    Attributes
    ----------
    id : int
        project id
    """
    id: int
